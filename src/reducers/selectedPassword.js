import {  } from "../actions"

function selectedPassword(state = { selectedPassword: []},action) {
    switch (action.type) {
      default:
        return state
    }
  }
  
  export default selectedPassword